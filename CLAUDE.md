# CLAUDE.md

## Workflow Guidelines

- Work on the existing `dev-v2` branch unless the user explicitly asks for another branch.
- After each completed small task or change batch, run the relevant review/checks, then create a git commit for the verified changes.
- Keep commits scoped to the completed task; do not mix unrelated pre-existing working-tree changes into the commit.
